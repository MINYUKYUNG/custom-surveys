import { QuestionTitle, QuestionForm } from '../components';

function Create() {
  return (
    <div className="Create">
      <QuestionTitle />
      <QuestionForm />
    </div>
  );
}

export default Create;
