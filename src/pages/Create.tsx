import { CreateTitle, CreateQuestion } from '../components';

function Create() {
  return (
    <div className="Create">
      <CreateTitle />
      <CreateQuestion />
    </div>
  );
}

export default Create;
